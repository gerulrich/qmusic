# Test configuration
app.version=test
tdl.client.id=test-client-id
tdl.refresh.token=test-refresh-token
tdl.master.key=test-master-key
tdl.domain=https://test.example.com

# Use random port for tests to avoid conflicts
quarkus.http.test-port=0
